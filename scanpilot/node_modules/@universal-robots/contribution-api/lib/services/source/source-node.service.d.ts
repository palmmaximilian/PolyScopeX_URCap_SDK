import { SourcesNode } from '../../types/nodes';
import { MessageInvokerService, UREventTarget } from '../message-invoker';
export declare const SOURCE_NODE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.SourceNodeService.";
/**
 * Service that handles updating application nodes
 */
export declare class SourceNodeService extends MessageInvokerService {
    readonly eventTarget: Worker | UREventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Update the source node
     * @param node New source node state
     */
    updateNode(node: SourcesNode): Promise<void>;
}
