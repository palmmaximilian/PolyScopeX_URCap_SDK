import { ApplicationNode } from '../../types/nodes';
import { UREventTarget } from '../message-invoker';
export declare const APPLICATION_NODE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.ApplicationNodeService.";
/**
 * Service that handles updating application nodes
 */
export declare class ApplicationNodeService {
    private readonly eventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: UREventTarget);
    /**
     * Update the program node
     * @param node New program node state
     */
    updateNode(node: ApplicationNode): Promise<void>;
}
