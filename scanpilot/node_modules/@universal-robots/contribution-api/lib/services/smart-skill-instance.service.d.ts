import { SmartSkillInstance } from '../types/nodes';
import { UREventTarget } from './message-invoker';
export declare const SMART_SKILL_INSTANCE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.SmartSkillInstanceService.";
/**
 * Service that handles updating smart skill instances
 */
export declare class SmartSkillInstanceService {
    private readonly eventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Update the program node
     * @param instance New smart skill instance data
     */
    updateInstance(instance: SmartSkillInstance): Promise<void>;
}
