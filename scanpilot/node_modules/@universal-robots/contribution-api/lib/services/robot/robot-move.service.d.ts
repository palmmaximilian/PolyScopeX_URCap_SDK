import { MoveScreenOptions } from '../../types/context';
import { JointPositions, Waypoint } from '../../types/nodes';
import { MessageInvokerService, UREventTarget } from '../message-invoker';
export declare const ROBOT_MOVE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.RobotMoveService.";
/**
 * RobotMoveService handles movement screen navigation
 */
export declare class RobotMoveService extends MessageInvokerService {
    readonly eventTarget: Worker | UREventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Opens the move screen and returns the selected position of the robot. Returns undefined if cancel is selected
     * @param options Move screen options
     */
    openMoveScreen(options: MoveScreenOptions): Promise<Waypoint | undefined>;
    /**
     * Opens the autoMove screen
     * @param targetPosition Joint vector to move to
     */
    autoMove(targetPosition: JointPositions | Waypoint): Promise<void>;
}
