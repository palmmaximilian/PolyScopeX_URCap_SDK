import { Observable } from 'rxjs';
import { MessageInvokerService, UREventTarget } from '../message-invoker';
export declare const FREEDRIVE_STATE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.FreedriveStateService.";
/**
 * Provides access to freedrive information on the robot
 */
export declare class FreedriveService extends MessageInvokerService {
    readonly eventTarget: Worker | UREventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Get the freedrive state
     *
     * @returns {Observable<boolean>} An observable that emits `true` if the robot is in freedrive state
     */
    isInFreedrive(): Observable<boolean>;
}
