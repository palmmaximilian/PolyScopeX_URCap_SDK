import { Observable } from 'rxjs';
import { RobotSettings } from '../../types/common';
import { MessageInvokerService, UREventTarget } from '../message-invoker';
export declare const SETTINGS_MESSAGE_TYPE_BASE = "ur.web.message.SettingsService.";
/**
 *
 */
export declare class SettingsService extends MessageInvokerService {
    readonly eventTarget: Worker | UREventTarget;
    /**
     *
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Get the current robot settings as an observable
     */
    getRobotSettings(): Observable<RobotSettings>;
    /**
     * Get the current robot settings once
     */
    getRobotSettingsOnce(): Promise<RobotSettings>;
}
