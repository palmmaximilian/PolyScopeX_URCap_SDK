import { ProgramNode } from '../../types/nodes';
import { UREventTarget } from '../message-invoker';
export declare const PROGRAM_NODE_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.ProgramNodeService.";
/**
 * Service that handles node insertion and removal og nodes
 */
export declare class ProgramNodeService {
    private readonly eventTarget;
    private readonly selectedNodeId;
    /**
     * @constructor
     * @param eventTarget
     * @param selectedNodeId
     */
    constructor(eventTarget: Worker | UREventTarget, selectedNodeId: string);
    /**
     * Update the program node
     * @param node New program node state
     */
    updateNode(node: ProgramNode): Promise<void>;
}
