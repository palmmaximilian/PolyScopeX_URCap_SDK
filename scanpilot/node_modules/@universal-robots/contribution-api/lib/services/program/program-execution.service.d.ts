import { Observable } from 'rxjs';
import { MessageInvokerService, UREventTarget } from '../message-invoker';
export declare const PROGRAM_EXECUTION_SERVICE_MESSAGE_TYPE_BASE = "ur.web.message.ProgramExecutionService.";
/**
 * Service that handles program execution
 */
export declare class ProgramExecutionService extends MessageInvokerService {
    readonly eventTarget: Worker | UREventTarget;
    /**
     * @constructor
     * @param eventTarget
     */
    constructor(eventTarget: Worker | UREventTarget);
    /**
     * Stop the program
     */
    stopProgram(): Promise<void>;
    /**
     * Play the program
     */
    playProgram(): Promise<void>;
    /**
     * Resumes the program
     */
    resumeProgram(): Promise<void>;
    /**
     * Pause the program
     */
    pauseProgram(): Promise<void>;
    /**
     * Get the Program Status
     */
    getProgramStatus(): Observable<string>;
    /**
     * Step to the next line in the program
     */
    stepProgram(): Promise<void>;
}
