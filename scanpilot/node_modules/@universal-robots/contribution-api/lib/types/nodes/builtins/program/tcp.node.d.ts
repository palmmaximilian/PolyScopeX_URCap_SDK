import { NodeType, ProgramNode } from '../..';
import { TabInputModel } from '../../../common';
import { Angle, Length } from '../../../values';
export interface TcpNode extends ProgramNode {
    type: NodeType.TCP;
    parameters: {
        endEffectorId?: string;
        tcpId?: string;
        useCustomTCP?: boolean;
        customTCP: {
            x: TabInputModel<Length>;
            y: TabInputModel<Length>;
            z: TabInputModel<Length>;
            rx: TabInputModel<Angle>;
            ry: TabInputModel<Angle>;
            rz: TabInputModel<Angle>;
        };
    };
}
