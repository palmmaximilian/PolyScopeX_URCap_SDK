import { NodeType, ProgramNode } from '../..';
import { TabInputModel } from '../../../common';
import { Length, Mass } from '../../../values';
export interface PayloadNode extends ProgramNode {
    type: NodeType.PAYLOAD;
    parameters: {
        customPayload: boolean;
        endEffectorId?: string;
        payload: TabInputModel<Mass>;
        centerOfGravity: {
            cx: TabInputModel<Length>;
            cy: TabInputModel<Length>;
            cz: TabInputModel<Length>;
        };
    };
}
