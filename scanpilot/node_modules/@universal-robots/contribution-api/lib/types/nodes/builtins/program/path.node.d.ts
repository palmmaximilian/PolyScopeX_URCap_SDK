import { NodeType, ProgramNode, Waypoint, WaypointTabInputModel } from '../..';
import { TabInputModel } from '../../../common';
import { AngularSpeed } from '../../../values';
export interface PathNode extends ProgramNode {
    type: NodeType.PATH;
    parameters: {
        waypoint?: WaypointTabInputModel;
        advanced: PathAdvancedSettings;
        path: Waypoint[];
        downSampleEpsilon: number;
        convertToMoveTo: boolean;
        pathLength: number;
    };
}
export interface PathAdvancedSettings {
    speed: PathSpeedSettings;
}
export interface PathSpeedSettings {
    speed: TabInputModel<AngularSpeed>;
}
