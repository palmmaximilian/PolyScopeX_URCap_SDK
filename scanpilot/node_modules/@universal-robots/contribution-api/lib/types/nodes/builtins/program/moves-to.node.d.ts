import { MotionProfile, MoveType, NodeType, ProgramNode, URVariable, WaypointTabInputModel } from '../..';
import { TabInputModel } from '../../../common';
import { Acceleration, AngularAcceleration, AngularSpeed, Length, Speed } from '../../../values';
export interface MovesToNode extends ProgramNode {
    type: NodeType.JOINT_MOVE | NodeType.LINEAR_MOVE;
    parameters: {
        motionProfile: MotionProfile;
        moveType: MoveType;
        variable: TabInputModel<URVariable | undefined>;
        waypoint?: WaypointTabInputModel;
        advanced: MovesToAdvancedSettings;
    };
}
export interface MovesToAdvancedSettings {
    speed: MovesToSpeedSettings;
    transform: MovesToTransformSettings;
    blend: MovesToBlendSettings;
}
export interface MovesToSpeedSettings {
    speed: TabInputModel<Speed | AngularSpeed>;
    acceleration: TabInputModel<Acceleration | AngularAcceleration>;
    motionValue: string;
    optiMoveSpeed: number;
    optiMoveAcceleration: number;
}
export interface MovesToBlendSettings {
    enabled: boolean;
    radius?: TabInputModel<Length>;
}
export interface MovesToTransformSettings {
    transform: boolean;
    poseVariable?: URVariable;
}
