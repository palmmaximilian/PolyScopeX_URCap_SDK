import { ProgramNode } from '../..';
import { TabInputModel } from '../../../common';
import { Acceleration, AngularAcceleration, AngularSpeed, Speed } from '../../../values';
import { NodeType } from './program-node-type';
export interface MoveNode extends ProgramNode {
    type: NodeType.MOVE;
    parameters: {
        moveType: 'moveJ' | 'moveL';
        joint: {
            speed: TabInputModel<AngularSpeed>;
            acceleration: TabInputModel<AngularAcceleration>;
        };
        linear: {
            speed: TabInputModel<Speed>;
            acceleration: TabInputModel<Acceleration>;
        };
        frameId: string;
    };
}
