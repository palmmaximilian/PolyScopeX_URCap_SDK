import { ApplicationNode } from '../../application.node';
import { Mounting } from '../../models/application';
import { ApplicationNodeType } from './application-node-type';
/**
 * Interface for the Mounting node.
 */
export interface MountingNode extends ApplicationNode {
    type: ApplicationNodeType.MOUNTING;
    mounting: Mounting;
}
