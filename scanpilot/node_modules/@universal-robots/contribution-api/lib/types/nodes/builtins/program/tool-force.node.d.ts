import { ProgramNode } from '../..';
import { TabInputModel } from '../../../common';
import { Force, Time } from '../../../values';
import { NodeType } from './program-node-type';
export interface ToolForceNode extends ProgramNode {
    type: NodeType.TOOL_FORCE;
    parameters: {
        x: {
            isValid: boolean;
            model: TabInputModel<ToolForceDirection>;
        };
        y: {
            isValid: boolean;
            model: TabInputModel<ToolForceDirection>;
        };
        z: {
            isValid: boolean;
            model: TabInputModel<ToolForceDirection>;
        };
        zeroSensor: {
            isValid: boolean;
            model: TabInputModel<ToolForceZeroSensor>;
        };
        selectedFrame: {
            isValid: boolean;
            model: TabInputModel<ToolForceFrame>;
        };
    };
}
export interface ToolForceDirection {
    enabled: boolean;
    force?: Force;
}
export interface ToolForceZeroSensor {
    enabled: boolean;
    waiting: Time;
}
export interface ToolForceFrame {
    enabled: boolean;
    frameName: string;
}
