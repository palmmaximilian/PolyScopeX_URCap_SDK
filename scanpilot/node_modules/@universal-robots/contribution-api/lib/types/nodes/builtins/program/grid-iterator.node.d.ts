import { ProgramNode } from '../../program.node';
import { URVariable } from '../../ur-symbol';
import { NodeType } from './program-node-type';
export interface GridIteratorNode extends ProgramNode {
    type: NodeType.GRID_ITERATOR;
    parameters: {
        action: 'increment' | 'reset';
        gridVariable?: URVariable;
    };
    lockChildren: false;
    allowsChildren: false;
}
