import { ProgramNode, URVariable } from '../..';
import { NodeType } from './program-node-type';
export interface LoopNode extends ProgramNode {
    type: NodeType.LOOP;
    parameters: {
        type: 'always' | 'xtimes' | 'expression';
        expression?: string;
        loopVariable?: URVariable;
        xtimes?: number;
    };
}
