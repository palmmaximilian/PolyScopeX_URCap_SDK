import { ApplicationNode, URVariable, Waypoint } from '../..';
export interface GridPatternParameters {
    grid: URVariable;
    waypoint: URVariable;
    corners: [Waypoint?, Waypoint?, Waypoint?, Waypoint?];
    numRows: number;
    numColumns: number;
}
export interface GridPatternNode extends ApplicationNode {
    grids: Array<GridPatternParameters>;
}
