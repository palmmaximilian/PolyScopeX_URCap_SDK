import { URVariable } from '../../ur-symbol';
import { Pose } from '../program';
import { FrameName } from './frame-name.model';
/**
 * Interface describing a Frame which contains a name along with a pose
 */
export interface Frame {
    name: FrameName;
    nameVariable?: URVariable;
    pose: Pose;
    parent?: FrameName;
}
