import { OptionalPromise } from '../behavior';
/**
 * LifeCycleEvent is a function that is called when a node is deleted
 */
export type LifeCycleEvent<eventType, dataType> = (event: eventType, data: dataType) => OptionalPromise<dataType | undefined>;
