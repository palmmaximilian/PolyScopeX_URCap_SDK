import { SerializerImplementation } from 'threads';
/**
 * Creates the serializer for the InsertionContext. Handles serializing and deserializing of the object.
 * @constructor
 */
export declare const InsertionContextSerializer: () => SerializerImplementation;
