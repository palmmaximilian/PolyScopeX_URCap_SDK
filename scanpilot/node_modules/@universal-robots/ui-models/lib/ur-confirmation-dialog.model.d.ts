import { DialogSizeType } from './ur-dialog.model';
export interface ConfirmationDialog {
    title: string;
    size?: DialogSizeType;
    icon?: string;
    showCancelButton?: boolean;
    cancelText?: string;
    confirmText?: string;
    disableConfirm?: boolean;
    text: string;
    critical?: boolean;
}
